apiVersion: v1
kind: Service
metadata:
  name: seaweedfs-s3
  namespace: seaweedfs-s3
  labels:
    app: seaweedfs
spec:
  ports:
    - port: 8333
      targetPort: 8333
      name: s3
  selector:
    app: seaweedfs
